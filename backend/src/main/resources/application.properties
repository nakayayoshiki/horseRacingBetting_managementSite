spring.application.name=keiba-tracker
server.port=${PORT:8080}
turso.database-url=${TURSO_DATABASE_URL}
turso.auth-token=${TURSO_AUTH_TOKEN}
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}
